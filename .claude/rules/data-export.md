# 数据导出规范

## 输出目录

- 文件统一保存到 `./output/`（不存在时自动创建）

## 文件命名

- 格式：`{简要描述}_{YYYYMMDD}.csv`
- 示例：`会员消费记录_20260609.csv`

## 编码

- CSV 文件使用 **UTF-8 with BOM**（Excel 直接打开不乱码）

## 样本确认

- 导出超过 1000 行的数据前，**必须**先展示前 10 行让用户确认

## 大数量警告

- 预计结果超过 10,000 行时，提醒用户并建议加 `LIMIT` 或分批导出
